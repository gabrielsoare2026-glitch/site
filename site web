import 'package:flutter/material.dart';
import 'feed.dart';
import 'login.dart';

void main() {
  runApp(Zitik());
}

class Zitik extends StatelessWidget {
  @override
  Widget build(BuildContext conte
  xt) {
    return MaterialApp(
      title: 'Zitik',
      theme: ThemeData.dark(),
      home: LoginPage(),
    );
  }
}import 'package:flutter/material.dart';

class LoginPage extends StatelessWidget {

  void loginGoogle() {
    print("Login com Google");
  }

  void loginFacebook() {
    print("Login com Facebook");
  }

  void loginPhone() {
    print("Login com celular");
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [

          Text("Zitik", style: TextStyle(fontSize: 40)),

          ElevatedButton(
            onPressed: loginGoogle,
            child: Text("Entrar com Google"),
          ),

          ElevatedButton(
            onPressed: loginFacebook,
            child: Text("Entrar com Facebook"),
          ),

          ElevatedButton(
            onPressed: loginPhone,
            child: Text("Entrar com Celular"),
          ),

        ],
      ),
      ),
    );
  }
}import 'package:flutter/material.dart';
import 'package:video_player/video_player.dart';

class FeedPage extends StatefulWidget {
  @override
  _FeedPageState createState() => _FeedPageState();
}

class _FeedPageState extends State<FeedPage> {

  final List videos = [
    "videos/video1.mp4",
    "videos/video2.mp4",
    "videos/video3.mp4",
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: PageView.builder(
        scrollDirection: Axis.vertical,
        itemCount: videos.length,
        itemBuilder: (context, index) {

          return Container(
            color: Colors.black,
            child: Center(
              child: Text("Video ${index+1}"),
            ),
          );

        },
      ),
    );
  }
}